name: currency-rate-bot

entryPoint: main.sc

botEngine: v2
language: ru

tests:
  include:
    - test.xml
    
messages:
    onError:
      defaultMessage: Ой, что-то пошло не так... Нужно переформулировать запрос.
